import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
    height: 200px;
`;

const Rank = () => {
    return <Wrapper>위에 3개 순위 목록</Wrapper>;
};

export default Rank;
